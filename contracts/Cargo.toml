[package]
name = "aequitas_contracts"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
odra = "0.25.0"
casper-types = "1.5.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
strip = true
